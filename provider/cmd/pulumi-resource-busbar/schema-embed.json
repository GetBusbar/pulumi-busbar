{
    "name": "busbar",
    "displayName": "Busbar",
    "description": "A Pulumi package for creating and managing Busbar LLM gateway resources through its admin API.",
    "keywords": [
        "pulumi",
        "busbar",
        "llm",
        "gateway",
        "ai",
        "category/utility"
    ],
    "homepage": "https://getbusbar.com",
    "license": "Apache-2.0",
    "attribution": "This Pulumi package is based on the [`busbar` Terraform Provider](https://github.com/GetBusbar/terraform-provider-busbar).",
    "repository": "https://github.com/getbusbar/pulumi-busbar",
    "publisher": "GetBusbar",
    "meta": {
        "moduleFormat": "(.*)(?:/[^/]*)"
    },
    "language": {
        "go": {
            "importBasePath": "github.com/getbusbar/pulumi-busbar/sdk/go/busbar",
            "generateResourceContainerTypes": true,
            "generateExtraInputTypes": true
        },
        "nodejs": {
            "packageName": "@getbusbar/pulumi-busbar",
            "packageDescription": "A Pulumi package for creating and managing Busbar LLM gateway resources through its admin API.",
            "readme": "\u003e This provider is a derived work of the [Terraform Provider](https://github.com/GetBusbar/terraform-provider-busbar)\n\u003e distributed under [MPL 2.0](https://www.mozilla.org/en-US/MPL/2.0/). If you encounter a bug or missing feature,\n\u003e first check the [`pulumi-busbar` repo](https://github.com/getbusbar/pulumi-busbar/issues); however, if that doesn't turn up anything,\n\u003e please consult the source [`terraform-provider-busbar` repo](https://github.com/GetBusbar/terraform-provider-busbar/issues).",
            "dependencies": {
                "@pulumi/pulumi": "^3.0.0"
            },
            "devDependencies": {
                "@types/node": "^18"
            },
            "compatibility": "tfbridge20",
            "disableUnionOutputTypes": true
        },
        "python": {
            "packageName": "pulumi_busbar",
            "requires": {
                "pulumi": "\u003e=3.0.0,\u003c4.0.0"
            },
            "readme": "\u003e This provider is a derived work of the [Terraform Provider](https://github.com/GetBusbar/terraform-provider-busbar)\n\u003e distributed under [MPL 2.0](https://www.mozilla.org/en-US/MPL/2.0/). If you encounter a bug or missing feature,\n\u003e first check the [`pulumi-busbar` repo](https://github.com/getbusbar/pulumi-busbar/issues); however, if that doesn't turn up anything,\n\u003e please consult the source [`terraform-provider-busbar` repo](https://github.com/GetBusbar/terraform-provider-busbar/issues).",
            "compatibility": "tfbridge20",
            "pyproject": {}
        }
    },
    "config": {
        "variables": {
            "caCertPem": {
                "type": "string",
                "description": "PEM-encoded CA certificate to trust the admin server's certificate (e.g. a private CA)."
            },
            "clientCertPem": {
                "type": "string",
                "description": "PEM-encoded client certificate for mTLS against a cert-gated admin plane."
            },
            "clientKeyPem": {
                "type": "string",
                "description": "PEM-encoded private key for client_cert_pem.",
                "secret": true
            },
            "endpoint": {
                "type": "string",
                "description": "Admin listener URL, e.g. https://busbar-admin.internal:8081. May also be set with the BUSBAR_ENDPOINT environment variable.",
                "defaultInfo": {
                    "environment": [
                        "BUSBAR_ENDPOINT"
                    ]
                }
            },
            "insecure": {
                "type": "boolean",
                "description": "Skip TLS certificate verification. Development only; never use against production."
            },
            "token": {
                "type": "string",
                "description": "Operator admin token, sent as the x-admin-token header. May also be set with the BUSBAR_ADMIN_TOKEN environment variable.",
                "defaultInfo": {
                    "environment": [
                        "BUSBAR_ADMIN_TOKEN"
                    ]
                },
                "secret": true
            }
        }
    },
    "provider": {
        "description": "The provider type for the busbar package. By default, resources use package-wide configuration\nsettings, however an explicit `Provider` instance may be created and passed during resource\nconstruction to achieve fine-grained programmatic control over provider settings. See the\n[documentation](https://www.pulumi.com/docs/reference/programming-model/#providers) for more information.\n",
        "properties": {
            "caCertPem": {
                "type": "string",
                "description": "PEM-encoded CA certificate to trust the admin server's certificate (e.g. a private CA)."
            },
            "clientCertPem": {
                "type": "string",
                "description": "PEM-encoded client certificate for mTLS against a cert-gated admin plane."
            },
            "clientKeyPem": {
                "type": "string",
                "description": "PEM-encoded private key for client_cert_pem.",
                "secret": true
            },
            "endpoint": {
                "type": "string",
                "description": "Admin listener URL, e.g. https://busbar-admin.internal:8081. May also be set with the BUSBAR_ENDPOINT environment variable."
            },
            "insecure": {
                "type": "boolean",
                "description": "Skip TLS certificate verification. Development only; never use against production."
            },
            "token": {
                "type": "string",
                "description": "Operator admin token, sent as the x-admin-token header. May also be set with the BUSBAR_ADMIN_TOKEN environment variable.",
                "secret": true
            }
        },
        "inputProperties": {
            "caCertPem": {
                "type": "string",
                "description": "PEM-encoded CA certificate to trust the admin server's certificate (e.g. a private CA)."
            },
            "clientCertPem": {
                "type": "string",
                "description": "PEM-encoded client certificate for mTLS against a cert-gated admin plane."
            },
            "clientKeyPem": {
                "type": "string",
                "description": "PEM-encoded private key for client_cert_pem.",
                "secret": true
            },
            "endpoint": {
                "type": "string",
                "description": "Admin listener URL, e.g. https://busbar-admin.internal:8081. May also be set with the BUSBAR_ENDPOINT environment variable.",
                "defaultInfo": {
                    "environment": [
                        "BUSBAR_ENDPOINT"
                    ]
                }
            },
            "insecure": {
                "type": "boolean",
                "description": "Skip TLS certificate verification. Development only; never use against production."
            },
            "token": {
                "type": "string",
                "description": "Operator admin token, sent as the x-admin-token header. May also be set with the BUSBAR_ADMIN_TOKEN environment variable.",
                "defaultInfo": {
                    "environment": [
                        "BUSBAR_ADMIN_TOKEN"
                    ]
                },
                "secret": true
            }
        },
        "methods": {
            "terraformConfig": "pulumi:providers:busbar/terraformConfig"
        }
    },
    "resources": {
        "busbar:index/config:Config": {
            "description": "A GitOps singleton that owns the running busbar config and applies it wholesale (POST /api/v1/admin/config/apply). Manage at most ONE of these per gateway. The document is the full JSON config payload busbar boots from ({\"config\": {...}, \"providers\": {...}}); applying it bumps config_version. Applies are LIVE-ONLY by default — they revert to disk truth on the next reload or restart unless the gateway persists an overlay. Destroying this resource is a no-op on the gateway (there is no unapply); it only drops Terraform's tracking.\n\n## Example Usage\n\n\u003c!--Start PulumiCodeChooser --\u003e\n```typescript\nimport * as pulumi from \"@pulumi/pulumi\";\nimport * as busbar from \"@getbusbar/pulumi-busbar\";\n\n// GitOps singleton: apply the whole running config document. Manage AT MOST ONE\n// busbar_config per gateway. The document is the JSON form of busbar's 1.5.0\n// config syntax, an envelope of { config = {config.yaml deploy block},\n// providers = {providers.yaml document} }.\n//\n// Applies are live-only by default: they revert to disk truth on the next reload\n// or restart unless the gateway persists an overlay. Destroying this resource is a\n// no-op on the gateway (there is no \"unapply\"); it only drops Terraform's tracking.\nconst running = new busbar.Config(\"running\", {document: JSON.stringify({\n    config: {\n        auth: {\n            chain: [\"keys\"],\n            admin_auth: [{\n                \"admin-tokens\": {\n                    token: {\n                        env: \"BUSBAR_ADMIN_TOKEN\",\n                    },\n                },\n            }],\n        },\n        providers: {\n            anthropic: {\n                api_key: {\n                    env: \"ANTHROPIC_API_KEY\",\n                },\n            },\n        },\n        models: {\n            \"claude-sonnet\": {\n                provider: \"anthropic\",\n            },\n        },\n        groups: {\n            \"team-checkout\": {},\n        },\n    },\n    providers: {\n        anthropic: {\n            protocol: \"anthropic\",\n            base_url: \"https://api.anthropic.com\",\n        },\n    },\n})});\nexport const configVersion = running.configVersion;\n```\n```python\nimport pulumi\nimport json\nimport pulumi_busbar as busbar\n\n# GitOps singleton: apply the whole running config document. Manage AT MOST ONE\n# busbar_config per gateway. The document is the JSON form of busbar's 1.5.0\n# config syntax, an envelope of { config = {config.yaml deploy block},\n# providers = {providers.yaml document} }.\n#\n# Applies are live-only by default: they revert to disk truth on the next reload\n# or restart unless the gateway persists an overlay. Destroying this resource is a\n# no-op on the gateway (there is no \"unapply\"); it only drops Terraform's tracking.\nrunning = busbar.Config(\"running\", document=json.dumps({\n    \"config\": {\n        \"auth\": {\n            \"chain\": [\"keys\"],\n            \"admin_auth\": [{\n                \"admin-tokens\": {\n                    \"token\": {\n                        \"env\": \"BUSBAR_ADMIN_TOKEN\",\n                    },\n                },\n            }],\n        },\n        \"providers\": {\n            \"anthropic\": {\n                \"api_key\": {\n                    \"env\": \"ANTHROPIC_API_KEY\",\n                },\n            },\n        },\n        \"models\": {\n            \"claude-sonnet\": {\n                \"provider\": \"anthropic\",\n            },\n        },\n        \"groups\": {\n            \"team-checkout\": {},\n        },\n    },\n    \"providers\": {\n        \"anthropic\": {\n            \"protocol\": \"anthropic\",\n            \"base_url\": \"https://api.anthropic.com\",\n        },\n    },\n}))\npulumi.export(\"configVersion\", running.config_version)\n```\n```csharp\nusing System.Collections.Generic;\nusing System.Linq;\nusing System.Text.Json;\nusing Pulumi;\nusing Busbar = Pulumi.Busbar;\n\nreturn await Deployment.RunAsync(() =\u003e \n{\n    // GitOps singleton: apply the whole running config document. Manage AT MOST ONE\n    // busbar_config per gateway. The document is the JSON form of busbar's 1.5.0\n    // config syntax, an envelope of { config = {config.yaml deploy block},\n    // providers = {providers.yaml document} }.\n    //\n    // Applies are live-only by default: they revert to disk truth on the next reload\n    // or restart unless the gateway persists an overlay. Destroying this resource is a\n    // no-op on the gateway (there is no \"unapply\"); it only drops Terraform's tracking.\n    var running = new Busbar.Config(\"running\", new()\n    {\n        Document = JsonSerializer.Serialize(new Dictionary\u003cstring, object?\u003e\n        {\n            [\"config\"] = new Dictionary\u003cstring, object?\u003e\n            {\n                [\"auth\"] = new Dictionary\u003cstring, object?\u003e\n                {\n                    [\"chain\"] = new[]\n                    {\n                        \"keys\",\n                    },\n                    [\"admin_auth\"] = new[]\n                    {\n                        new Dictionary\u003cstring, object?\u003e\n                        {\n                            [\"admin-tokens\"] = new Dictionary\u003cstring, object?\u003e\n                            {\n                                [\"token\"] = new Dictionary\u003cstring, object?\u003e\n                                {\n                                    [\"env\"] = \"BUSBAR_ADMIN_TOKEN\",\n                                },\n                            },\n                        },\n                    },\n                },\n                [\"providers\"] = new Dictionary\u003cstring, object?\u003e\n                {\n                    [\"anthropic\"] = new Dictionary\u003cstring, object?\u003e\n                    {\n                        [\"api_key\"] = new Dictionary\u003cstring, object?\u003e\n                        {\n                            [\"env\"] = \"ANTHROPIC_API_KEY\",\n                        },\n                    },\n                },\n                [\"models\"] = new Dictionary\u003cstring, object?\u003e\n                {\n                    [\"claude-sonnet\"] = new Dictionary\u003cstring, object?\u003e\n                    {\n                        [\"provider\"] = \"anthropic\",\n                    },\n                },\n                [\"groups\"] = new Dictionary\u003cstring, object?\u003e\n                {\n                    [\"team-checkout\"] = new Dictionary\u003cstring, object?\u003e\n                    {\n                    },\n                },\n            },\n            [\"providers\"] = new Dictionary\u003cstring, object?\u003e\n            {\n                [\"anthropic\"] = new Dictionary\u003cstring, object?\u003e\n                {\n                    [\"protocol\"] = \"anthropic\",\n                    [\"base_url\"] = \"https://api.anthropic.com\",\n                },\n            },\n        }),\n    });\n\n    return new Dictionary\u003cstring, object?\u003e\n    {\n        [\"configVersion\"] = running.ConfigVersion,\n    };\n});\n```\n```go\npackage main\n\nimport (\n\t\"encoding/json\"\n\n\t\"github.com/getbusbar/pulumi-busbar/sdk/go/busbar\"\n\t\"github.com/pulumi/pulumi/sdk/v3/go/pulumi\"\n)\n\nfunc main() {\n\tpulumi.Run(func(ctx *pulumi.Context) error {\n\t\ttmpJSON0, err := json.Marshal(map[string]interface{}{\n\t\t\t\"config\": map[string]interface{}{\n\t\t\t\t\"auth\": map[string]interface{}{\n\t\t\t\t\t\"chain\": []string{\n\t\t\t\t\t\t\"keys\",\n\t\t\t\t\t},\n\t\t\t\t\t\"admin_auth\": []map[string]interface{}{\n\t\t\t\t\t\tmap[string]interface{}{\n\t\t\t\t\t\t\t\"admin-tokens\": map[string]interface{}{\n\t\t\t\t\t\t\t\t\"token\": map[string]interface{}{\n\t\t\t\t\t\t\t\t\t\"env\": \"BUSBAR_ADMIN_TOKEN\",\n\t\t\t\t\t\t\t\t},\n\t\t\t\t\t\t\t},\n\t\t\t\t\t\t},\n\t\t\t\t\t},\n\t\t\t\t},\n\t\t\t\t\"providers\": map[string]interface{}{\n\t\t\t\t\t\"anthropic\": map[string]interface{}{\n\t\t\t\t\t\t\"api_key\": map[string]interface{}{\n\t\t\t\t\t\t\t\"env\": \"ANTHROPIC_API_KEY\",\n\t\t\t\t\t\t},\n\t\t\t\t\t},\n\t\t\t\t},\n\t\t\t\t\"models\": map[string]interface{}{\n\t\t\t\t\t\"claude-sonnet\": map[string]interface{}{\n\t\t\t\t\t\t\"provider\": \"anthropic\",\n\t\t\t\t\t},\n\t\t\t\t},\n\t\t\t\t\"groups\": map[string]interface{}{\n\t\t\t\t\t\"team-checkout\": map[string]interface{}{},\n\t\t\t\t},\n\t\t\t},\n\t\t\t\"providers\": map[string]interface{}{\n\t\t\t\t\"anthropic\": map[string]interface{}{\n\t\t\t\t\t\"protocol\": \"anthropic\",\n\t\t\t\t\t\"base_url\": \"https://api.anthropic.com\",\n\t\t\t\t},\n\t\t\t},\n\t\t})\n\t\tif err != nil {\n\t\t\treturn err\n\t\t}\n\t\tjson0 := string(tmpJSON0)\n\t\t// GitOps singleton: apply the whole running config document. Manage AT MOST ONE\n\t\t// busbar_config per gateway. The document is the JSON form of busbar's 1.5.0\n\t\t// config syntax, an envelope of { config = {config.yaml deploy block},\n\t\t// providers = {providers.yaml document} }.\n\t\t//\n\t\t// Applies are live-only by default: they revert to disk truth on the next reload\n\t\t// or restart unless the gateway persists an overlay. Destroying this resource is a\n\t\t// no-op on the gateway (there is no \"unapply\"); it only drops Terraform's tracking.\n\t\trunning, err := busbar.NewConfig(ctx, \"running\", \u0026busbar.ConfigArgs{\n\t\t\tDocument: pulumi.String(json0),\n\t\t})\n\t\tif err != nil {\n\t\t\treturn err\n\t\t}\n\t\tctx.Export(\"configVersion\", running.ConfigVersion)\n\t\treturn nil\n\t})\n}\n```\n```hcl\npulumi {\n  required_providers {\n    busbar = {\n      source = \"pulumi/busbar\"\n    }\n  }\n}\n\n# GitOps singleton: apply the whole running config document. Manage AT MOST ONE\n# busbar_config per gateway. The document is the JSON form of busbar's 1.5.0\n# config syntax, an envelope of { config = {config.yaml deploy block},\n# providers = {providers.yaml document} }.\n#\n# Applies are live-only by default: they revert to disk truth on the next reload\n# or restart unless the gateway persists an overlay. Destroying this resource is a\n# no-op on the gateway (there is no \"unapply\"); it only drops Terraform's tracking.\nresource \"busbar_config\" \"running\" {\n  document = jsonencode({\n    \"config\" = {\n      \"auth\" = {\n        \"chain\" = [\"keys\"]\n        \"admin_auth\" = [{\n          \"admin-tokens\" = {\n            \"token\" = {\n              \"env\" = \"BUSBAR_ADMIN_TOKEN\"\n            }\n          }\n        }]\n      }\n      \"providers\" = {\n        \"anthropic\" = {\n          \"api_key\" = {\n            \"env\" = \"ANTHROPIC_API_KEY\"\n          }\n        }\n      }\n      \"models\" = {\n        \"claude-sonnet\" = {\n          \"provider\" = \"anthropic\"\n        }\n      }\n      \"groups\" = {\n        \"team-checkout\" = {}\n      }\n    }\n    \"providers\" = {\n      \"anthropic\" = {\n        \"protocol\" = \"anthropic\"\n        \"base_url\" = \"https://api.anthropic.com\"\n      }\n    }\n  })\n}\noutput \"configVersion\" {\n  value = busbar_config.running.config_version\n}\n```\n```java\npackage generated_program;\n\nimport com.pulumi.Context;\nimport com.pulumi.Pulumi;\nimport com.pulumi.core.Output;\nimport com.pulumi.busbar.Config;\nimport com.pulumi.busbar.ConfigArgs;\nimport static com.pulumi.codegen.internal.Serialization.*;\nimport java.util.ArrayList;\nimport java.util.Arrays;\nimport java.util.Map;\nimport java.io.File;\nimport java.nio.file.Files;\nimport java.nio.file.Paths;\n\npublic class App {\n    public static void main(String[] args) {\n        Pulumi.run(App::stack);\n    }\n\n    public static void stack(Context ctx) {\n        // GitOps singleton: apply the whole running config document. Manage AT MOST ONE\n        // busbar_config per gateway. The document is the JSON form of busbar's 1.5.0\n        // config syntax, an envelope of { config = {config.yaml deploy block},\n        // providers = {providers.yaml document} }.\n        //\n        // Applies are live-only by default: they revert to disk truth on the next reload\n        // or restart unless the gateway persists an overlay. Destroying this resource is a\n        // no-op on the gateway (there is no \"unapply\"); it only drops Terraform's tracking.\n        var running = new Config(\"running\", ConfigArgs.builder()\n            .document(serializeJson(\n                jsonObject(\n                    jsonProperty(\"config\", jsonObject(\n                        jsonProperty(\"auth\", jsonObject(\n                            jsonProperty(\"chain\", jsonArray(\"keys\")),\n                            jsonProperty(\"admin_auth\", jsonArray(jsonObject(\n                                jsonProperty(\"admin-tokens\", jsonObject(\n                                    jsonProperty(\"token\", jsonObject(\n                                        jsonProperty(\"env\", \"BUSBAR_ADMIN_TOKEN\")\n                                    ))\n                                ))\n                            )))\n                        )),\n                        jsonProperty(\"providers\", jsonObject(\n                            jsonProperty(\"anthropic\", jsonObject(\n                                jsonProperty(\"api_key\", jsonObject(\n                                    jsonProperty(\"env\", \"ANTHROPIC_API_KEY\")\n                                ))\n                            ))\n                        )),\n                        jsonProperty(\"models\", jsonObject(\n                            jsonProperty(\"claude-sonnet\", jsonObject(\n                                jsonProperty(\"provider\", \"anthropic\")\n                            ))\n                        )),\n                        jsonProperty(\"groups\", jsonObject(\n                            jsonProperty(\"team-checkout\", jsonObject(\n\n                            ))\n                        ))\n                    )),\n                    jsonProperty(\"providers\", jsonObject(\n                        jsonProperty(\"anthropic\", jsonObject(\n                            jsonProperty(\"protocol\", \"anthropic\"),\n                            jsonProperty(\"base_url\", \"https://api.anthropic.com\")\n                        ))\n                    ))\n                )))\n            .build());\n\n        ctx.export(\"configVersion\", running.configVersion());\n    }\n}\n```\n```yaml\nresources:\n  # GitOps singleton: apply the whole running config document. Manage AT MOST ONE\n  # busbar_config per gateway. The document is the JSON form of busbar's 1.5.0\n  # config syntax, an envelope of { config = {config.yaml deploy block},\n  # providers = {providers.yaml document} }.\n  #\n  # Applies are live-only by default: they revert to disk truth on the next reload\n  # or restart unless the gateway persists an overlay. Destroying this resource is a\n  # no-op on the gateway (there is no \"unapply\"); it only drops Terraform's tracking.\n  running:\n    type: busbar:Config\n    properties:\n      document:\n        fn::toJSON:\n          config:\n            auth:\n              chain:\n                - keys\n              admin_auth:\n                - admin-tokens:\n                    token:\n                      env: BUSBAR_ADMIN_TOKEN\n            providers:\n              anthropic:\n                api_key:\n                  env: ANTHROPIC_API_KEY\n            models:\n              claude-sonnet:\n                provider: anthropic\n            groups:\n              team-checkout: {}\n          providers:\n            anthropic:\n              protocol: anthropic\n              base_url: https://api.anthropic.com\noutputs:\n  configVersion: ${running.configVersion}\n```\n\u003c!--End PulumiCodeChooser --\u003e\n\n## Import\n\nThe `pulumi import` command can be used, for example:\n\nThe config singleton has a fixed import id. The document is not recoverable\n(GET /config is a redacted projection), so supply the matching \u003cspan pulumi-lang-nodejs=\"`document`\" pulumi-lang-dotnet=\"`Document`\" pulumi-lang-go=\"`document`\" pulumi-lang-python=\"`document`\" pulumi-lang-yaml=\"`document`\" pulumi-lang-java=\"`document`\" pulumi-lang-hcl=\"`document`\"\u003e`document`\u003c/span\u003e in\nyour configuration after import; only\u003cspan pulumi-lang-nodejs=\" configVersion \" pulumi-lang-dotnet=\" ConfigVersion \" pulumi-lang-go=\" configVersion \" pulumi-lang-python=\" config_version \" pulumi-lang-yaml=\" configVersion \" pulumi-lang-java=\" configVersion \" pulumi-lang-hcl=\" config_version \"\u003e configVersion \u003c/span\u003eis read live.\n\n```sh\n$ pulumi import busbar:index/config:Config running config\n```\n\n",
            "properties": {
                "configVersion": {
                    "type": "integer",
                    "description": "The monotonic config version after the last apply. Process-local; resets to 0 on gateway restart.\n"
                },
                "document": {
                    "type": "string",
                    "description": "The full config apply document as a JSON string: {\"config\": {DeployCfg}, \"providers\": {name: ProviderDef}}. The \u003cspan pulumi-lang-nodejs=\"`config`\" pulumi-lang-dotnet=\"`Config`\" pulumi-lang-go=\"`config`\" pulumi-lang-python=\"`config`\" pulumi-lang-yaml=\"`config`\" pulumi-lang-java=\"`config`\" pulumi-lang-hcl=\"`config`\"\u003e`config`\u003c/span\u003e key is required; \u003cspan pulumi-lang-nodejs=\"`providers`\" pulumi-lang-dotnet=\"`Providers`\" pulumi-lang-go=\"`providers`\" pulumi-lang-python=\"`providers`\" pulumi-lang-yaml=\"`providers`\" pulumi-lang-java=\"`providers`\" pulumi-lang-hcl=\"`providers`\"\u003e`providers`\u003c/span\u003e is optional. This is the write model (the boot-file shape), NOT the redacted read projection returned by GET /config.\n"
                }
            },
            "required": [
                "configVersion",
                "document"
            ],
            "inputProperties": {
                "document": {
                    "type": "string",
                    "description": "The full config apply document as a JSON string: {\"config\": {DeployCfg}, \"providers\": {name: ProviderDef}}. The \u003cspan pulumi-lang-nodejs=\"`config`\" pulumi-lang-dotnet=\"`Config`\" pulumi-lang-go=\"`config`\" pulumi-lang-python=\"`config`\" pulumi-lang-yaml=\"`config`\" pulumi-lang-java=\"`config`\" pulumi-lang-hcl=\"`config`\"\u003e`config`\u003c/span\u003e key is required; \u003cspan pulumi-lang-nodejs=\"`providers`\" pulumi-lang-dotnet=\"`Providers`\" pulumi-lang-go=\"`providers`\" pulumi-lang-python=\"`providers`\" pulumi-lang-yaml=\"`providers`\" pulumi-lang-java=\"`providers`\" pulumi-lang-hcl=\"`providers`\"\u003e`providers`\u003c/span\u003e is optional. This is the write model (the boot-file shape), NOT the redacted read projection returned by GET /config.\n"
                }
            },
            "requiredInputs": [
                "document"
            ],
            "stateInputs": {
                "description": "Input properties used for looking up and filtering Config resources.\n",
                "properties": {
                    "configVersion": {
                        "type": "integer",
                        "description": "The monotonic config version after the last apply. Process-local; resets to 0 on gateway restart.\n"
                    },
                    "document": {
                        "type": "string",
                        "description": "The full config apply document as a JSON string: {\"config\": {DeployCfg}, \"providers\": {name: ProviderDef}}. The \u003cspan pulumi-lang-nodejs=\"`config`\" pulumi-lang-dotnet=\"`Config`\" pulumi-lang-go=\"`config`\" pulumi-lang-python=\"`config`\" pulumi-lang-yaml=\"`config`\" pulumi-lang-java=\"`config`\" pulumi-lang-hcl=\"`config`\"\u003e`config`\u003c/span\u003e key is required; \u003cspan pulumi-lang-nodejs=\"`providers`\" pulumi-lang-dotnet=\"`Providers`\" pulumi-lang-go=\"`providers`\" pulumi-lang-python=\"`providers`\" pulumi-lang-yaml=\"`providers`\" pulumi-lang-java=\"`providers`\" pulumi-lang-hcl=\"`providers`\"\u003e`providers`\u003c/span\u003e is optional. This is the write model (the boot-file shape), NOT the redacted read projection returned by GET /config.\n"
                    }
                },
                "type": "object"
            }
        },
        "busbar:index/hook:Hook": {
            "description": "A routing hook (busbar \u003e= 1.5.0): a tap or gate backed by a signed `kind: hook` plugin, wired into busbar's request/ranking pipeline (POST/GET/PUT/DELETE /api/v1/admin/hooks). The grant fields (kind, prompt, user) are immutable once registered — changing them replaces the hook.\n\n## Example Usage\n\n\u003c!--Start PulumiCodeChooser --\u003e\n```typescript\nimport * as pulumi from \"@pulumi/pulumi\";\nimport * as busbar from \"@getbusbar/pulumi-busbar\";\n\n// Register a blocking \"gate\" hook backed by a signed `kind: hook` plugin from\n// the gateway's plugin catalog: it may inspect (prompt = \"ro\") and rerank\n// candidates before the request is dispatched.\nconst ranker = new busbar.Hook(\"ranker\", {\n    name: \"quality-ranker\",\n    kind: \"gate\",\n    plugin: \"ranking\",\n    prompt: \"ro\",\n    timeoutMs: 100,\n    priority: 10,\n    onError: \"weighted\",\n    settings: JSON.stringify({\n        min_score: 0.6,\n    }),\n});\n// A fire-and-forget \"tap\" hook for async usage telemetry.\nconst usageTap = new busbar.Hook(\"usage_tap\", {\n    name: \"usage-telemetry\",\n    kind: \"tap\",\n    plugin: \"usage-telemetry\",\n    at: \"completion\",\n    global: true,\n});\n```\n```python\nimport pulumi\nimport json\nimport pulumi_busbar as busbar\n\n# Register a blocking \"gate\" hook backed by a signed `kind: hook` plugin from\n# the gateway's plugin catalog: it may inspect (prompt = \"ro\") and rerank\n# candidates before the request is dispatched.\nranker = busbar.Hook(\"ranker\",\n    name=\"quality-ranker\",\n    kind=\"gate\",\n    plugin=\"ranking\",\n    prompt=\"ro\",\n    timeout_ms=100,\n    priority=10,\n    on_error=\"weighted\",\n    settings=json.dumps({\n        \"min_score\": 0.6,\n    }))\n# A fire-and-forget \"tap\" hook for async usage telemetry.\nusage_tap = busbar.Hook(\"usage_tap\",\n    name=\"usage-telemetry\",\n    kind=\"tap\",\n    plugin=\"usage-telemetry\",\n    at=\"completion\",\n    global_=True)\n```\n```csharp\nusing System.Collections.Generic;\nusing System.Linq;\nusing System.Text.Json;\nusing Pulumi;\nusing Busbar = Pulumi.Busbar;\n\nreturn await Deployment.RunAsync(() =\u003e \n{\n    // Register a blocking \"gate\" hook backed by a signed `kind: hook` plugin from\n    // the gateway's plugin catalog: it may inspect (prompt = \"ro\") and rerank\n    // candidates before the request is dispatched.\n    var ranker = new Busbar.Hook(\"ranker\", new()\n    {\n        Name = \"quality-ranker\",\n        Kind = \"gate\",\n        Plugin = \"ranking\",\n        Prompt = \"ro\",\n        TimeoutMs = 100,\n        Priority = 10,\n        OnError = \"weighted\",\n        Settings = JsonSerializer.Serialize(new Dictionary\u003cstring, object?\u003e\n        {\n            [\"min_score\"] = 0.6,\n        }),\n    });\n\n    // A fire-and-forget \"tap\" hook for async usage telemetry.\n    var usageTap = new Busbar.Hook(\"usage_tap\", new()\n    {\n        Name = \"usage-telemetry\",\n        Kind = \"tap\",\n        Plugin = \"usage-telemetry\",\n        At = \"completion\",\n        Global = true,\n    });\n\n});\n```\n```go\npackage main\n\nimport (\n\t\"encoding/json\"\n\n\t\"github.com/getbusbar/pulumi-busbar/sdk/go/busbar\"\n\t\"github.com/pulumi/pulumi/sdk/v3/go/pulumi\"\n)\n\nfunc main() {\n\tpulumi.Run(func(ctx *pulumi.Context) error {\n\t\ttmpJSON0, err := json.Marshal(map[string]interface{}{\n\t\t\t\"min_score\": 0.6,\n\t\t})\n\t\tif err != nil {\n\t\t\treturn err\n\t\t}\n\t\tjson0 := string(tmpJSON0)\n\t\t// Register a blocking \"gate\" hook backed by a signed `kind: hook` plugin from\n\t\t// the gateway's plugin catalog: it may inspect (prompt = \"ro\") and rerank\n\t\t// candidates before the request is dispatched.\n\t\t_, err = busbar.NewHook(ctx, \"ranker\", \u0026busbar.HookArgs{\n\t\t\tName:      pulumi.String(\"quality-ranker\"),\n\t\t\tKind:      pulumi.String(\"gate\"),\n\t\t\tPlugin:    pulumi.String(\"ranking\"),\n\t\t\tPrompt:    pulumi.String(\"ro\"),\n\t\t\tTimeoutMs: pulumi.Int(100),\n\t\t\tPriority:  pulumi.Int(10),\n\t\t\tOnError:   pulumi.String(\"weighted\"),\n\t\t\tSettings:  pulumi.String(json0),\n\t\t})\n\t\tif err != nil {\n\t\t\treturn err\n\t\t}\n\t\t// A fire-and-forget \"tap\" hook for async usage telemetry.\n\t\t_, err = busbar.NewHook(ctx, \"usage_tap\", \u0026busbar.HookArgs{\n\t\t\tName:   pulumi.String(\"usage-telemetry\"),\n\t\t\tKind:   pulumi.String(\"tap\"),\n\t\t\tPlugin: pulumi.String(\"usage-telemetry\"),\n\t\t\tAt:     pulumi.String(\"completion\"),\n\t\t\tGlobal: pulumi.Bool(true),\n\t\t})\n\t\tif err != nil {\n\t\t\treturn err\n\t\t}\n\t\treturn nil\n\t})\n}\n```\n```hcl\npulumi {\n  required_providers {\n    busbar = {\n      source = \"pulumi/busbar\"\n    }\n  }\n}\n\n# Register a blocking \"gate\" hook backed by a signed `kind: hook` plugin from\n# the gateway's plugin catalog: it may inspect (prompt = \"ro\") and rerank\n# candidates before the request is dispatched.\nresource \"busbar_hook\" \"ranker\" {\n  name       = \"quality-ranker\"\n  kind       = \"gate\"\n  plugin     = \"ranking\"\n  prompt     = \"ro\"\n  timeout_ms = 100\n  priority   = 10\n  on_error   = \"weighted\"\n  settings = jsonencode({\n    \"min_score\" = 0.6\n  })\n}\n# A fire-and-forget \"tap\" hook for async usage telemetry.\nresource \"busbar_hook\" \"usage_tap\" {\n  name   = \"usage-telemetry\"\n  kind   = \"tap\"\n  plugin = \"usage-telemetry\"\n  at     = \"completion\"\n  global = true\n}\n```\n```java\npackage generated_program;\n\nimport com.pulumi.Context;\nimport com.pulumi.Pulumi;\nimport com.pulumi.core.Output;\nimport com.pulumi.busbar.Hook;\nimport com.pulumi.busbar.HookArgs;\nimport static com.pulumi.codegen.internal.Serialization.*;\nimport java.util.ArrayList;\nimport java.util.Arrays;\nimport java.util.Map;\nimport java.io.File;\nimport java.nio.file.Files;\nimport java.nio.file.Paths;\n\npublic class App {\n    public static void main(String[] args) {\n        Pulumi.run(App::stack);\n    }\n\n    public static void stack(Context ctx) {\n        // Register a blocking \"gate\" hook backed by a signed `kind: hook` plugin from\n        // the gateway's plugin catalog: it may inspect (prompt = \"ro\") and rerank\n        // candidates before the request is dispatched.\n        var ranker = new Hook(\"ranker\", HookArgs.builder()\n            .name(\"quality-ranker\")\n            .kind(\"gate\")\n            .plugin(\"ranking\")\n            .prompt(\"ro\")\n            .timeoutMs(100)\n            .priority(10)\n            .onError(\"weighted\")\n            .settings(serializeJson(\n                jsonObject(\n                    jsonProperty(\"min_score\", 0.6)\n                )))\n            .build());\n\n        // A fire-and-forget \"tap\" hook for async usage telemetry.\n        var usageTap = new Hook(\"usageTap\", HookArgs.builder()\n            .name(\"usage-telemetry\")\n            .kind(\"tap\")\n            .plugin(\"usage-telemetry\")\n            .at(\"completion\")\n            .global(true)\n            .build());\n\n    }\n}\n```\n```yaml\nresources:\n  # Register a blocking \"gate\" hook backed by a signed `kind: hook` plugin from\n  # the gateway's plugin catalog: it may inspect (prompt = \"ro\") and rerank\n  # candidates before the request is dispatched.\n  ranker:\n    type: busbar:Hook\n    properties:\n      name: quality-ranker\n      kind: gate\n      plugin: ranking\n      prompt: ro\n      timeoutMs: 100\n      priority: 10\n      onError: weighted\n      settings:\n        fn::toJSON:\n          min_score: 0.6\n  # A fire-and-forget \"tap\" hook for async usage telemetry.\n  usageTap:\n    type: busbar:Hook\n    name: usage_tap\n    properties:\n      name: usage-telemetry\n      kind: tap\n      plugin: usage-telemetry\n      at: completion\n      global: true\n```\n\u003c!--End PulumiCodeChooser --\u003e\n\n## Import\n\nThe `pulumi import` command can be used, for example:\n\nHooks are imported by name.\n\n```sh\n$ pulumi import busbar:index/hook:Hook ranker quality-ranker\n```\n\n",
            "properties": {
                "at": {
                    "type": "string",
                    "description": "Pipeline stage: request, route, attempt, or completion. Null lets busbar place it by kind.\n"
                },
                "default": {
                    "type": "boolean",
                    "description": "Whether this hook is the default for its stage. Write-only (not echoed by reads).\n"
                },
                "global": {
                    "type": "boolean",
                    "description": "Whether the hook applies globally (all pools). Defaults to false. May read back true if wired via global_hooks.\n"
                },
                "kind": {
                    "type": "string",
                    "description": "Transport contract: tap (fire-and-forget, non-blocking) or gate (blocking, may rewrite/reject). Immutable grant; changing it replaces the hook.\n"
                },
                "name": {
                    "type": "string",
                    "description": "Unique hook name (\u003c= 256 chars; not a reserved terminal name). Immutable; changing it replaces the hook.\n"
                },
                "onEmpty": {
                    "type": "string",
                    "description": "Fallback policy when the hook yields an empty ranking: weighted, reject, or first. Write-only (not echoed by reads).\n"
                },
                "onError": {
                    "type": "string",
                    "description": "Behavior when the hook errors/times out: a terminal (weighted, reject, first, nothing) or another hook name. Defaults to nothing.\n"
                },
                "plugin": {
                    "type": "string",
                    "description": "The signed `kind: hook` plugin this hook dispatches to (its NAME from the gateway's plugin catalog, e.g. a compiled-in plugin such as \u003cspan pulumi-lang-nodejs=\"`ranking`\" pulumi-lang-dotnet=\"`Ranking`\" pulumi-lang-go=\"`ranking`\" pulumi-lang-python=\"`ranking`\" pulumi-lang-yaml=\"`ranking`\" pulumi-lang-java=\"`ranking`\" pulumi-lang-hcl=\"`ranking`\"\u003e`ranking`\u003c/span\u003e).\n"
                },
                "priority": {
                    "type": "integer",
                    "description": "Ordering priority within a stage. Defaults to 0.\n"
                },
                "prompt": {
                    "type": "string",
                    "description": "Prompt-content access grant: no, ro, or rw. Defaults to no. Immutable grant; changing it replaces the hook. (rw is invalid on a tap.)\n"
                },
                "settings": {
                    "type": "string",
                    "description": "Opaque per-hook settings as a JSON object string (\u003c= 64KiB, \u003c= 256 keys). Defaults to {}.\n"
                },
                "timeoutMs": {
                    "type": "integer",
                    "description": "Per-call timeout in milliseconds. Defaults to 1.\n"
                },
                "user": {
                    "type": "string",
                    "description": "Caller-identity access grant: no or ro. Defaults to no. Immutable grant; changing it replaces the hook.\n"
                }
            },
            "required": [
                "global",
                "kind",
                "name",
                "onError",
                "plugin",
                "priority",
                "prompt",
                "settings",
                "timeoutMs",
                "user"
            ],
            "inputProperties": {
                "at": {
                    "type": "string",
                    "description": "Pipeline stage: request, route, attempt, or completion. Null lets busbar place it by kind.\n"
                },
                "default": {
                    "type": "boolean",
                    "description": "Whether this hook is the default for its stage. Write-only (not echoed by reads).\n"
                },
                "global": {
                    "type": "boolean",
                    "description": "Whether the hook applies globally (all pools). Defaults to false. May read back true if wired via global_hooks.\n"
                },
                "kind": {
                    "type": "string",
                    "description": "Transport contract: tap (fire-and-forget, non-blocking) or gate (blocking, may rewrite/reject). Immutable grant; changing it replaces the hook.\n"
                },
                "name": {
                    "type": "string",
                    "description": "Unique hook name (\u003c= 256 chars; not a reserved terminal name). Immutable; changing it replaces the hook.\n"
                },
                "onEmpty": {
                    "type": "string",
                    "description": "Fallback policy when the hook yields an empty ranking: weighted, reject, or first. Write-only (not echoed by reads).\n"
                },
                "onError": {
                    "type": "string",
                    "description": "Behavior when the hook errors/times out: a terminal (weighted, reject, first, nothing) or another hook name. Defaults to nothing.\n"
                },
                "plugin": {
                    "type": "string",
                    "description": "The signed `kind: hook` plugin this hook dispatches to (its NAME from the gateway's plugin catalog, e.g. a compiled-in plugin such as \u003cspan pulumi-lang-nodejs=\"`ranking`\" pulumi-lang-dotnet=\"`Ranking`\" pulumi-lang-go=\"`ranking`\" pulumi-lang-python=\"`ranking`\" pulumi-lang-yaml=\"`ranking`\" pulumi-lang-java=\"`ranking`\" pulumi-lang-hcl=\"`ranking`\"\u003e`ranking`\u003c/span\u003e).\n"
                },
                "priority": {
                    "type": "integer",
                    "description": "Ordering priority within a stage. Defaults to 0.\n"
                },
                "prompt": {
                    "type": "string",
                    "description": "Prompt-content access grant: no, ro, or rw. Defaults to no. Immutable grant; changing it replaces the hook. (rw is invalid on a tap.)\n"
                },
                "settings": {
                    "type": "string",
                    "description": "Opaque per-hook settings as a JSON object string (\u003c= 64KiB, \u003c= 256 keys). Defaults to {}.\n"
                },
                "timeoutMs": {
                    "type": "integer",
                    "description": "Per-call timeout in milliseconds. Defaults to 1.\n"
                },
                "user": {
                    "type": "string",
                    "description": "Caller-identity access grant: no or ro. Defaults to no. Immutable grant; changing it replaces the hook.\n"
                }
            },
            "requiredInputs": [
                "kind",
                "plugin"
            ],
            "stateInputs": {
                "description": "Input properties used for looking up and filtering Hook resources.\n",
                "properties": {
                    "at": {
                        "type": "string",
                        "description": "Pipeline stage: request, route, attempt, or completion. Null lets busbar place it by kind.\n"
                    },
                    "default": {
                        "type": "boolean",
                        "description": "Whether this hook is the default for its stage. Write-only (not echoed by reads).\n"
                    },
                    "global": {
                        "type": "boolean",
                        "description": "Whether the hook applies globally (all pools). Defaults to false. May read back true if wired via global_hooks.\n"
                    },
                    "kind": {
                        "type": "string",
                        "description": "Transport contract: tap (fire-and-forget, non-blocking) or gate (blocking, may rewrite/reject). Immutable grant; changing it replaces the hook.\n"
                    },
                    "name": {
                        "type": "string",
                        "description": "Unique hook name (\u003c= 256 chars; not a reserved terminal name). Immutable; changing it replaces the hook.\n"
                    },
                    "onEmpty": {
                        "type": "string",
                        "description": "Fallback policy when the hook yields an empty ranking: weighted, reject, or first. Write-only (not echoed by reads).\n"
                    },
                    "onError": {
                        "type": "string",
                        "description": "Behavior when the hook errors/times out: a terminal (weighted, reject, first, nothing) or another hook name. Defaults to nothing.\n"
                    },
                    "plugin": {
                        "type": "string",
                        "description": "The signed `kind: hook` plugin this hook dispatches to (its NAME from the gateway's plugin catalog, e.g. a compiled-in plugin such as \u003cspan pulumi-lang-nodejs=\"`ranking`\" pulumi-lang-dotnet=\"`Ranking`\" pulumi-lang-go=\"`ranking`\" pulumi-lang-python=\"`ranking`\" pulumi-lang-yaml=\"`ranking`\" pulumi-lang-java=\"`ranking`\" pulumi-lang-hcl=\"`ranking`\"\u003e`ranking`\u003c/span\u003e).\n"
                    },
                    "priority": {
                        "type": "integer",
                        "description": "Ordering priority within a stage. Defaults to 0.\n"
                    },
                    "prompt": {
                        "type": "string",
                        "description": "Prompt-content access grant: no, ro, or rw. Defaults to no. Immutable grant; changing it replaces the hook. (rw is invalid on a tap.)\n"
                    },
                    "settings": {
                        "type": "string",
                        "description": "Opaque per-hook settings as a JSON object string (\u003c= 64KiB, \u003c= 256 keys). Defaults to {}.\n"
                    },
                    "timeoutMs": {
                        "type": "integer",
                        "description": "Per-call timeout in milliseconds. Defaults to 1.\n"
                    },
                    "user": {
                        "type": "string",
                        "description": "Caller-identity access grant: no or ro. Defaults to no. Immutable grant; changing it replaces the hook.\n"
                    }
                },
                "type": "object"
            }
        },
        "busbar:index/virtualKey:VirtualKey": {
            "description": "A governance virtual key (busbar \u003e= 1.5.0): a mintable, revocable, EXPIRING signed-token credential, optionally bound to a `groups:` bucket that carries all budget and rate enforcement (POST/GET/PATCH/DELETE /api/v1/admin/keys). The signed token is returned by busbar only once, at creation, and is stored in state as a sensitive value; refreshes update metadata (group/enabled/state) but never the token. Requires governance to be enabled on the gateway.\n\n## Example Usage\n\n\u003c!--Start PulumiCodeChooser --\u003e\n```typescript\nimport * as pulumi from \"@pulumi/pulumi\";\nimport * as busbar from \"@getbusbar/pulumi-busbar\";\n\n// Mint a governance virtual key (busbar \u003e= 1.5.0): a signed, expiring token,\n// bound to a `groups:` bucket that carries all budget/rate enforcement, scoped\n// to the \"smart\" pool. The signed token is returned only once, at creation, and\n// stored in state as a sensitive value.\nconst app = new busbar.VirtualKey(\"app\", {\n    name: \"checkout-service\",\n    group: \"team-checkout\",\n    allowedPools: [\"smart\"],\n    expiresIn: \"30d\",\n    labels: {\n        service: \"checkout\",\n    },\n});\nexport const appKeyToken = app.token;\n```\n```python\nimport pulumi\nimport pulumi_busbar as busbar\n\n# Mint a governance virtual key (busbar \u003e= 1.5.0): a signed, expiring token,\n# bound to a `groups:` bucket that carries all budget/rate enforcement, scoped\n# to the \"smart\" pool. The signed token is returned only once, at creation, and\n# stored in state as a sensitive value.\napp = busbar.VirtualKey(\"app\",\n    name=\"checkout-service\",\n    group=\"team-checkout\",\n    allowed_pools=[\"smart\"],\n    expires_in=\"30d\",\n    labels={\n        \"service\": \"checkout\",\n    })\npulumi.export(\"appKeyToken\", app.token)\n```\n```csharp\nusing System.Collections.Generic;\nusing System.Linq;\nusing Pulumi;\nusing Busbar = Pulumi.Busbar;\n\nreturn await Deployment.RunAsync(() =\u003e \n{\n    // Mint a governance virtual key (busbar \u003e= 1.5.0): a signed, expiring token,\n    // bound to a `groups:` bucket that carries all budget/rate enforcement, scoped\n    // to the \"smart\" pool. The signed token is returned only once, at creation, and\n    // stored in state as a sensitive value.\n    var app = new Busbar.VirtualKey(\"app\", new()\n    {\n        Name = \"checkout-service\",\n        Group = \"team-checkout\",\n        AllowedPools = new[]\n        {\n            \"smart\",\n        },\n        ExpiresIn = \"30d\",\n        Labels = \n        {\n            { \"service\", \"checkout\" },\n        },\n    });\n\n    return new Dictionary\u003cstring, object?\u003e\n    {\n        [\"appKeyToken\"] = app.Token,\n    };\n});\n```\n```go\npackage main\n\nimport (\n\t\"github.com/getbusbar/pulumi-busbar/sdk/go/busbar\"\n\t\"github.com/pulumi/pulumi/sdk/v3/go/pulumi\"\n)\n\nfunc main() {\n\tpulumi.Run(func(ctx *pulumi.Context) error {\n\t\t// Mint a governance virtual key (busbar \u003e= 1.5.0): a signed, expiring token,\n\t\t// bound to a `groups:` bucket that carries all budget/rate enforcement, scoped\n\t\t// to the \"smart\" pool. The signed token is returned only once, at creation, and\n\t\t// stored in state as a sensitive value.\n\t\tapp, err := busbar.NewVirtualKey(ctx, \"app\", \u0026busbar.VirtualKeyArgs{\n\t\t\tName:  pulumi.String(\"checkout-service\"),\n\t\t\tGroup: pulumi.String(\"team-checkout\"),\n\t\t\tAllowedPools: pulumi.StringArray{\n\t\t\t\tpulumi.String(\"smart\"),\n\t\t\t},\n\t\t\tExpiresIn: pulumi.String(\"30d\"),\n\t\t\tLabels: pulumi.StringMap{\n\t\t\t\t\"service\": pulumi.String(\"checkout\"),\n\t\t\t},\n\t\t})\n\t\tif err != nil {\n\t\t\treturn err\n\t\t}\n\t\tctx.Export(\"appKeyToken\", app.Token)\n\t\treturn nil\n\t})\n}\n```\n```hcl\npulumi {\n  required_providers {\n    busbar = {\n      source = \"pulumi/busbar\"\n    }\n  }\n}\n\n# Mint a governance virtual key (busbar \u003e= 1.5.0): a signed, expiring token,\n# bound to a `groups:` bucket that carries all budget/rate enforcement, scoped\n# to the \"smart\" pool. The signed token is returned only once, at creation, and\n# stored in state as a sensitive value.\nresource \"busbar_virtualkey\" \"app\" {\n  name          = \"checkout-service\"\n  group         = \"team-checkout\"\n  allowed_pools = [\"smart\"]\n  expires_in    = \"30d\"\n  labels = {\n    \"service\" = \"checkout\"\n  }\n}\n# The signed bearer token (bbk_...) — hand this to the calling application.\noutput \"appKeyToken\" {\n  value = busbar_virtualkey.app.token\n}\n```\n```java\npackage generated_program;\n\nimport com.pulumi.Context;\nimport com.pulumi.Pulumi;\nimport com.pulumi.core.Output;\nimport com.pulumi.busbar.VirtualKey;\nimport com.pulumi.busbar.VirtualKeyArgs;\nimport java.util.ArrayList;\nimport java.util.Arrays;\nimport java.util.Map;\nimport java.io.File;\nimport java.nio.file.Files;\nimport java.nio.file.Paths;\n\npublic class App {\n    public static void main(String[] args) {\n        Pulumi.run(App::stack);\n    }\n\n    public static void stack(Context ctx) {\n        // Mint a governance virtual key (busbar \u003e= 1.5.0): a signed, expiring token,\n        // bound to a `groups:` bucket that carries all budget/rate enforcement, scoped\n        // to the \"smart\" pool. The signed token is returned only once, at creation, and\n        // stored in state as a sensitive value.\n        var app = new VirtualKey(\"app\", VirtualKeyArgs.builder()\n            .name(\"checkout-service\")\n            .group(\"team-checkout\")\n            .allowedPools(\"smart\")\n            .expiresIn(\"30d\")\n            .labels(Map.of(\"service\", \"checkout\"))\n            .build());\n\n        ctx.export(\"appKeyToken\", app.token());\n    }\n}\n```\n```yaml\nresources:\n  # Mint a governance virtual key (busbar \u003e= 1.5.0): a signed, expiring token,\n  # bound to a `groups:` bucket that carries all budget/rate enforcement, scoped\n  # to the \"smart\" pool. The signed token is returned only once, at creation, and\n  # stored in state as a sensitive value.\n  app:\n    type: busbar:VirtualKey\n    properties:\n      name: checkout-service\n      group: team-checkout\n      allowedPools:\n        - smart\n      expiresIn: 30d\n      labels:\n        service: checkout\noutputs:\n  # The signed bearer token (bbk_...) — hand this to the calling application.\n  appKeyToken: ${app.token}\n```\n\u003c!--End PulumiCodeChooser --\u003e\n\n## Import\n\nThe `pulumi import` command can be used, for example:\n\nVirtual keys are imported by their server-assigned id (the signed token\ncannot be recovered — it is create-only — and stays null after import).\n\n```sh\n$ pulumi import busbar:index/virtualKey:VirtualKey app vk_0123456789abcdef\n```\n\n",
            "properties": {
                "allowedPools": {
                    "type": "array",
                    "items": {
                        "type": "string"
                    },
                    "description": "Pools this key may target. Omitted/null means ALL pools; an explicit empty list means NO pools. Immutable; changing it replaces the key.\n"
                },
                "awsAccessKeyId": {
                    "type": "string",
                    "description": "AWS-style access key id, when issue*aws*credential is true. Returned only at creation.\n"
                },
                "awsSecretAccessKey": {
                    "type": "string",
                    "description": "AWS-style secret access key, when issue*aws*credential is true. Returned only at creation; stored in state and never re-read.\n",
                    "secret": true
                },
                "createdAt": {
                    "type": "integer",
                    "description": "Epoch seconds the key was minted.\n"
                },
                "enabled": {
                    "type": "boolean",
                    "description": "Whether the key currently resolves. Defaults to true at mint. Mutable via PATCH (false = reversible disable; the key's \u003cspan pulumi-lang-nodejs=\"`state`\" pulumi-lang-dotnet=\"`State`\" pulumi-lang-go=\"`state`\" pulumi-lang-python=\"`state`\" pulumi-lang-yaml=\"`state`\" pulumi-lang-java=\"`state`\" pulumi-lang-hcl=\"`state`\"\u003e`state`\u003c/span\u003e reads \"disabled\").\n"
                },
                "expiresAt": {
                    "type": "integer",
                    "description": "Token expiry as absolute Unix seconds. May be set at mint (mutually exclusive with \u003cspan pulumi-lang-nodejs=\"`expiresIn`\" pulumi-lang-dotnet=\"`ExpiresIn`\" pulumi-lang-go=\"`expiresIn`\" pulumi-lang-python=\"`expires_in`\" pulumi-lang-yaml=\"`expiresIn`\" pulumi-lang-java=\"`expiresIn`\" pulumi-lang-hcl=\"`expires_in`\"\u003e`expiresIn`\u003c/span\u003e) and is always computed from the mint response. Changing it replaces the key.\n"
                },
                "expiresIn": {
                    "type": "string",
                    "description": "Token lifetime as a duration string (e.g. \u003cspan pulumi-lang-nodejs=\"`7d`\" pulumi-lang-dotnet=\"`7d`\" pulumi-lang-go=\"`7d`\" pulumi-lang-python=\"`7d`\" pulumi-lang-yaml=\"`7d`\" pulumi-lang-java=\"`7d`\" pulumi-lang-hcl=\"`7d`\"\u003e`7d`\u003c/span\u003e, \u003cspan pulumi-lang-nodejs=\"`24h`\" pulumi-lang-dotnet=\"`24h`\" pulumi-lang-go=\"`24h`\" pulumi-lang-python=\"`24h`\" pulumi-lang-yaml=\"`24h`\" pulumi-lang-java=\"`24h`\" pulumi-lang-hcl=\"`24h`\"\u003e`24h`\u003c/span\u003e, \u003cspan pulumi-lang-nodejs=\"`30m`\" pulumi-lang-dotnet=\"`30m`\" pulumi-lang-go=\"`30m`\" pulumi-lang-python=\"`30m`\" pulumi-lang-yaml=\"`30m`\" pulumi-lang-java=\"`30m`\" pulumi-lang-hcl=\"`30m`\"\u003e`30m`\u003c/span\u003e, \u003cspan pulumi-lang-nodejs=\"`3600s`\" pulumi-lang-dotnet=\"`3600s`\" pulumi-lang-go=\"`3600s`\" pulumi-lang-python=\"`3600s`\" pulumi-lang-yaml=\"`3600s`\" pulumi-lang-java=\"`3600s`\" pulumi-lang-hcl=\"`3600s`\"\u003e`3600s`\u003c/span\u003e); the token's expiry is mint-time + this. Mutually exclusive with \u003cspan pulumi-lang-nodejs=\"`expiresAt`\" pulumi-lang-dotnet=\"`ExpiresAt`\" pulumi-lang-go=\"`expiresAt`\" pulumi-lang-python=\"`expires_at`\" pulumi-lang-yaml=\"`expiresAt`\" pulumi-lang-java=\"`expiresAt`\" pulumi-lang-hcl=\"`expires_at`\"\u003e`expiresAt`\u003c/span\u003e. Omitting both applies the server default TTL. Write-only mint directive; changing it replaces the key.\n"
                },
                "group": {
                    "type": "string",
                    "description": "The `groups:` bucket this key binds to (at most one); all budget/rate enforcement flows through the group. The group must already exist unless \u003cspan pulumi-lang-nodejs=\"`parent`\" pulumi-lang-dotnet=\"`Parent`\" pulumi-lang-go=\"`parent`\" pulumi-lang-python=\"`parent`\" pulumi-lang-yaml=\"`parent`\" pulumi-lang-java=\"`parent`\" pulumi-lang-hcl=\"`parent`\"\u003e`parent`\u003c/span\u003e is set (auto-provision). Omit for an authed-but-unlimited key. Mutable via PATCH: changing it rebinds, removing it unbinds.\n"
                },
                "groupProvisioned": {
                    "type": "boolean",
                    "description": "Whether the mint auto-provisioned its bound group leaf (self-service). Known only at creation.\n"
                },
                "issueAwsCredential": {
                    "type": "boolean",
                    "description": "When true, also mint an AWS-style access-key-id + secret access key (SigV4/Bedrock inbound auth). Both are returned only at creation. Immutable.\n"
                },
                "labels": {
                    "type": "object",
                    "additionalProperties": {
                        "type": "string"
                    },
                    "description": "Mint-time labels echoed onto this key's metric series (Prometheus-safe names; never interpreted by enforcement). Immutable; changing them replaces the key.\n"
                },
                "name": {
                    "type": "string",
                    "description": "Human-readable label (\u003c= 256 chars). Immutable; changing it replaces the key.\n"
                },
                "parent": {
                    "type": "string",
                    "description": "Auto-provision target: the EXISTING parent group under which \u003cspan pulumi-lang-nodejs=\"`group`\" pulumi-lang-dotnet=\"`Group`\" pulumi-lang-go=\"`group`\" pulumi-lang-python=\"`group`\" pulumi-lang-yaml=\"`group`\" pulumi-lang-java=\"`group`\" pulumi-lang-hcl=\"`group`\"\u003e`group`\u003c/span\u003e is created as a leaf when it does not yet exist. Write-only mint directive (never echoed by reads). Changing it replaces the key.\n"
                },
                "state": {
                    "type": "string",
                    "description": "Lifecycle state derived by the server: active, disabled, revoked, or tombstoned. A tombstoned/revoked key is treated as gone and planned for recreation.\n"
                },
                "token": {
                    "type": "string",
                    "description": "The busbar-signed bearer token (bbk_...) — the key credential. Returned only at creation; stored in state and never re-read.\n",
                    "secret": true
                }
            },
            "required": [
                "awsAccessKeyId",
                "awsSecretAccessKey",
                "createdAt",
                "enabled",
                "expiresAt",
                "groupProvisioned",
                "name",
                "state",
                "token"
            ],
            "inputProperties": {
                "allowedPools": {
                    "type": "array",
                    "items": {
                        "type": "string"
                    },
                    "description": "Pools this key may target. Omitted/null means ALL pools; an explicit empty list means NO pools. Immutable; changing it replaces the key.\n"
                },
                "enabled": {
                    "type": "boolean",
                    "description": "Whether the key currently resolves. Defaults to true at mint. Mutable via PATCH (false = reversible disable; the key's \u003cspan pulumi-lang-nodejs=\"`state`\" pulumi-lang-dotnet=\"`State`\" pulumi-lang-go=\"`state`\" pulumi-lang-python=\"`state`\" pulumi-lang-yaml=\"`state`\" pulumi-lang-java=\"`state`\" pulumi-lang-hcl=\"`state`\"\u003e`state`\u003c/span\u003e reads \"disabled\").\n"
                },
                "expiresAt": {
                    "type": "integer",
                    "description": "Token expiry as absolute Unix seconds. May be set at mint (mutually exclusive with \u003cspan pulumi-lang-nodejs=\"`expiresIn`\" pulumi-lang-dotnet=\"`ExpiresIn`\" pulumi-lang-go=\"`expiresIn`\" pulumi-lang-python=\"`expires_in`\" pulumi-lang-yaml=\"`expiresIn`\" pulumi-lang-java=\"`expiresIn`\" pulumi-lang-hcl=\"`expires_in`\"\u003e`expiresIn`\u003c/span\u003e) and is always computed from the mint response. Changing it replaces the key.\n"
                },
                "expiresIn": {
                    "type": "string",
                    "description": "Token lifetime as a duration string (e.g. \u003cspan pulumi-lang-nodejs=\"`7d`\" pulumi-lang-dotnet=\"`7d`\" pulumi-lang-go=\"`7d`\" pulumi-lang-python=\"`7d`\" pulumi-lang-yaml=\"`7d`\" pulumi-lang-java=\"`7d`\" pulumi-lang-hcl=\"`7d`\"\u003e`7d`\u003c/span\u003e, \u003cspan pulumi-lang-nodejs=\"`24h`\" pulumi-lang-dotnet=\"`24h`\" pulumi-lang-go=\"`24h`\" pulumi-lang-python=\"`24h`\" pulumi-lang-yaml=\"`24h`\" pulumi-lang-java=\"`24h`\" pulumi-lang-hcl=\"`24h`\"\u003e`24h`\u003c/span\u003e, \u003cspan pulumi-lang-nodejs=\"`30m`\" pulumi-lang-dotnet=\"`30m`\" pulumi-lang-go=\"`30m`\" pulumi-lang-python=\"`30m`\" pulumi-lang-yaml=\"`30m`\" pulumi-lang-java=\"`30m`\" pulumi-lang-hcl=\"`30m`\"\u003e`30m`\u003c/span\u003e, \u003cspan pulumi-lang-nodejs=\"`3600s`\" pulumi-lang-dotnet=\"`3600s`\" pulumi-lang-go=\"`3600s`\" pulumi-lang-python=\"`3600s`\" pulumi-lang-yaml=\"`3600s`\" pulumi-lang-java=\"`3600s`\" pulumi-lang-hcl=\"`3600s`\"\u003e`3600s`\u003c/span\u003e); the token's expiry is mint-time + this. Mutually exclusive with \u003cspan pulumi-lang-nodejs=\"`expiresAt`\" pulumi-lang-dotnet=\"`ExpiresAt`\" pulumi-lang-go=\"`expiresAt`\" pulumi-lang-python=\"`expires_at`\" pulumi-lang-yaml=\"`expiresAt`\" pulumi-lang-java=\"`expiresAt`\" pulumi-lang-hcl=\"`expires_at`\"\u003e`expiresAt`\u003c/span\u003e. Omitting both applies the server default TTL. Write-only mint directive; changing it replaces the key.\n"
                },
                "group": {
                    "type": "string",
                    "description": "The `groups:` bucket this key binds to (at most one); all budget/rate enforcement flows through the group. The group must already exist unless \u003cspan pulumi-lang-nodejs=\"`parent`\" pulumi-lang-dotnet=\"`Parent`\" pulumi-lang-go=\"`parent`\" pulumi-lang-python=\"`parent`\" pulumi-lang-yaml=\"`parent`\" pulumi-lang-java=\"`parent`\" pulumi-lang-hcl=\"`parent`\"\u003e`parent`\u003c/span\u003e is set (auto-provision). Omit for an authed-but-unlimited key. Mutable via PATCH: changing it rebinds, removing it unbinds.\n"
                },
                "issueAwsCredential": {
                    "type": "boolean",
                    "description": "When true, also mint an AWS-style access-key-id + secret access key (SigV4/Bedrock inbound auth). Both are returned only at creation. Immutable.\n"
                },
                "labels": {
                    "type": "object",
                    "additionalProperties": {
                        "type": "string"
                    },
                    "description": "Mint-time labels echoed onto this key's metric series (Prometheus-safe names; never interpreted by enforcement). Immutable; changing them replaces the key.\n"
                },
                "name": {
                    "type": "string",
                    "description": "Human-readable label (\u003c= 256 chars). Immutable; changing it replaces the key.\n"
                },
                "parent": {
                    "type": "string",
                    "description": "Auto-provision target: the EXISTING parent group under which \u003cspan pulumi-lang-nodejs=\"`group`\" pulumi-lang-dotnet=\"`Group`\" pulumi-lang-go=\"`group`\" pulumi-lang-python=\"`group`\" pulumi-lang-yaml=\"`group`\" pulumi-lang-java=\"`group`\" pulumi-lang-hcl=\"`group`\"\u003e`group`\u003c/span\u003e is created as a leaf when it does not yet exist. Write-only mint directive (never echoed by reads). Changing it replaces the key.\n"
                }
            },
            "stateInputs": {
                "description": "Input properties used for looking up and filtering VirtualKey resources.\n",
                "properties": {
                    "allowedPools": {
                        "type": "array",
                        "items": {
                            "type": "string"
                        },
                        "description": "Pools this key may target. Omitted/null means ALL pools; an explicit empty list means NO pools. Immutable; changing it replaces the key.\n"
                    },
                    "awsAccessKeyId": {
                        "type": "string",
                        "description": "AWS-style access key id, when issue*aws*credential is true. Returned only at creation.\n"
                    },
                    "awsSecretAccessKey": {
                        "type": "string",
                        "description": "AWS-style secret access key, when issue*aws*credential is true. Returned only at creation; stored in state and never re-read.\n",
                        "secret": true
                    },
                    "createdAt": {
                        "type": "integer",
                        "description": "Epoch seconds the key was minted.\n"
                    },
                    "enabled": {
                        "type": "boolean",
                        "description": "Whether the key currently resolves. Defaults to true at mint. Mutable via PATCH (false = reversible disable; the key's \u003cspan pulumi-lang-nodejs=\"`state`\" pulumi-lang-dotnet=\"`State`\" pulumi-lang-go=\"`state`\" pulumi-lang-python=\"`state`\" pulumi-lang-yaml=\"`state`\" pulumi-lang-java=\"`state`\" pulumi-lang-hcl=\"`state`\"\u003e`state`\u003c/span\u003e reads \"disabled\").\n"
                    },
                    "expiresAt": {
                        "type": "integer",
                        "description": "Token expiry as absolute Unix seconds. May be set at mint (mutually exclusive with \u003cspan pulumi-lang-nodejs=\"`expiresIn`\" pulumi-lang-dotnet=\"`ExpiresIn`\" pulumi-lang-go=\"`expiresIn`\" pulumi-lang-python=\"`expires_in`\" pulumi-lang-yaml=\"`expiresIn`\" pulumi-lang-java=\"`expiresIn`\" pulumi-lang-hcl=\"`expires_in`\"\u003e`expiresIn`\u003c/span\u003e) and is always computed from the mint response. Changing it replaces the key.\n"
                    },
                    "expiresIn": {
                        "type": "string",
                        "description": "Token lifetime as a duration string (e.g. \u003cspan pulumi-lang-nodejs=\"`7d`\" pulumi-lang-dotnet=\"`7d`\" pulumi-lang-go=\"`7d`\" pulumi-lang-python=\"`7d`\" pulumi-lang-yaml=\"`7d`\" pulumi-lang-java=\"`7d`\" pulumi-lang-hcl=\"`7d`\"\u003e`7d`\u003c/span\u003e, \u003cspan pulumi-lang-nodejs=\"`24h`\" pulumi-lang-dotnet=\"`24h`\" pulumi-lang-go=\"`24h`\" pulumi-lang-python=\"`24h`\" pulumi-lang-yaml=\"`24h`\" pulumi-lang-java=\"`24h`\" pulumi-lang-hcl=\"`24h`\"\u003e`24h`\u003c/span\u003e, \u003cspan pulumi-lang-nodejs=\"`30m`\" pulumi-lang-dotnet=\"`30m`\" pulumi-lang-go=\"`30m`\" pulumi-lang-python=\"`30m`\" pulumi-lang-yaml=\"`30m`\" pulumi-lang-java=\"`30m`\" pulumi-lang-hcl=\"`30m`\"\u003e`30m`\u003c/span\u003e, \u003cspan pulumi-lang-nodejs=\"`3600s`\" pulumi-lang-dotnet=\"`3600s`\" pulumi-lang-go=\"`3600s`\" pulumi-lang-python=\"`3600s`\" pulumi-lang-yaml=\"`3600s`\" pulumi-lang-java=\"`3600s`\" pulumi-lang-hcl=\"`3600s`\"\u003e`3600s`\u003c/span\u003e); the token's expiry is mint-time + this. Mutually exclusive with \u003cspan pulumi-lang-nodejs=\"`expiresAt`\" pulumi-lang-dotnet=\"`ExpiresAt`\" pulumi-lang-go=\"`expiresAt`\" pulumi-lang-python=\"`expires_at`\" pulumi-lang-yaml=\"`expiresAt`\" pulumi-lang-java=\"`expiresAt`\" pulumi-lang-hcl=\"`expires_at`\"\u003e`expiresAt`\u003c/span\u003e. Omitting both applies the server default TTL. Write-only mint directive; changing it replaces the key.\n"
                    },
                    "group": {
                        "type": "string",
                        "description": "The `groups:` bucket this key binds to (at most one); all budget/rate enforcement flows through the group. The group must already exist unless \u003cspan pulumi-lang-nodejs=\"`parent`\" pulumi-lang-dotnet=\"`Parent`\" pulumi-lang-go=\"`parent`\" pulumi-lang-python=\"`parent`\" pulumi-lang-yaml=\"`parent`\" pulumi-lang-java=\"`parent`\" pulumi-lang-hcl=\"`parent`\"\u003e`parent`\u003c/span\u003e is set (auto-provision). Omit for an authed-but-unlimited key. Mutable via PATCH: changing it rebinds, removing it unbinds.\n"
                    },
                    "groupProvisioned": {
                        "type": "boolean",
                        "description": "Whether the mint auto-provisioned its bound group leaf (self-service). Known only at creation.\n"
                    },
                    "issueAwsCredential": {
                        "type": "boolean",
                        "description": "When true, also mint an AWS-style access-key-id + secret access key (SigV4/Bedrock inbound auth). Both are returned only at creation. Immutable.\n"
                    },
                    "labels": {
                        "type": "object",
                        "additionalProperties": {
                            "type": "string"
                        },
                        "description": "Mint-time labels echoed onto this key's metric series (Prometheus-safe names; never interpreted by enforcement). Immutable; changing them replaces the key.\n"
                    },
                    "name": {
                        "type": "string",
                        "description": "Human-readable label (\u003c= 256 chars). Immutable; changing it replaces the key.\n"
                    },
                    "parent": {
                        "type": "string",
                        "description": "Auto-provision target: the EXISTING parent group under which \u003cspan pulumi-lang-nodejs=\"`group`\" pulumi-lang-dotnet=\"`Group`\" pulumi-lang-go=\"`group`\" pulumi-lang-python=\"`group`\" pulumi-lang-yaml=\"`group`\" pulumi-lang-java=\"`group`\" pulumi-lang-hcl=\"`group`\"\u003e`group`\u003c/span\u003e is created as a leaf when it does not yet exist. Write-only mint directive (never echoed by reads). Changing it replaces the key.\n"
                    },
                    "state": {
                        "type": "string",
                        "description": "Lifecycle state derived by the server: active, disabled, revoked, or tombstoned. A tombstoned/revoked key is treated as gone and planned for recreation.\n"
                    },
                    "token": {
                        "type": "string",
                        "description": "The busbar-signed bearer token (bbk_...) — the key credential. Returned only at creation; stored in state and never re-read.\n",
                        "secret": true
                    }
                },
                "type": "object"
            }
        }
    },
    "functions": {
        "busbar:index/getInfo:getInfo": {
            "description": "Read-only view of the target busbar gateway: version, the compiled-in plugin proof, uptime, and topology counts (GET /api/v1/admin/info).\n\n## Example Usage\n\n\u003c!--Start PulumiCodeChooser --\u003e\n```typescript\nimport * as pulumi from \"@pulumi/pulumi\";\nimport * as busbar from \"@getbusbar/pulumi-busbar\";\n\n// Read the target gateway's version, compiled-in plugin proof, and topology.\nconst current = busbar.getInfo({});\nexport const busbarVersion = current.then(current =\u003e current.version);\nexport const compiledAuthModules = current.then(current =\u003e current.authModules);\n```\n```python\nimport pulumi\nimport pulumi_busbar as busbar\n\n# Read the target gateway's version, compiled-in plugin proof, and topology.\ncurrent = busbar.get_info()\npulumi.export(\"busbarVersion\", current.version)\npulumi.export(\"compiledAuthModules\", current.auth_modules)\n```\n```csharp\nusing System.Collections.Generic;\nusing System.Linq;\nusing Pulumi;\nusing Busbar = Pulumi.Busbar;\n\nreturn await Deployment.RunAsync(() =\u003e \n{\n    // Read the target gateway's version, compiled-in plugin proof, and topology.\n    var current = Busbar.GetInfo.Invoke();\n\n    return new Dictionary\u003cstring, object?\u003e\n    {\n        [\"busbarVersion\"] = current.Apply(getInfoResult =\u003e getInfoResult.Version),\n        [\"compiledAuthModules\"] = current.Apply(getInfoResult =\u003e getInfoResult.AuthModules),\n    };\n});\n```\n```go\npackage main\n\nimport (\n\t\"github.com/getbusbar/pulumi-busbar/sdk/go/busbar\"\n\t\"github.com/pulumi/pulumi/sdk/v3/go/pulumi\"\n)\n\nfunc main() {\n\tpulumi.Run(func(ctx *pulumi.Context) error {\n\t\t// Read the target gateway's version, compiled-in plugin proof, and topology.\n\t\tcurrent, err := busbar.GetInfo(ctx, map[string]interface{}{}, nil)\n\t\tif err != nil {\n\t\t\treturn err\n\t\t}\n\t\tctx.Export(\"busbarVersion\", current.Version)\n\t\tctx.Export(\"compiledAuthModules\", current.AuthModules)\n\t\treturn nil\n\t})\n}\n```\n```hcl\npulumi {\n  required_providers {\n    busbar = {\n      source = \"pulumi/busbar\"\n    }\n  }\n}\n\ndata \"busbar_getinfo\" \"current\" {\n}\n\n# Read the target gateway's version, compiled-in plugin proof, and topology.\noutput \"busbarVersion\" {\n  value = data.busbar_getinfo.current.version\n}\noutput \"compiledAuthModules\" {\n  value = data.busbar_getinfo.current.auth_modules\n}\n```\n```java\npackage generated_program;\n\nimport com.pulumi.Context;\nimport com.pulumi.Pulumi;\nimport com.pulumi.core.Output;\nimport com.pulumi.busbar.BusbarFunctions;\nimport java.util.ArrayList;\nimport java.util.Arrays;\nimport java.util.Map;\nimport java.io.File;\nimport java.nio.file.Files;\nimport java.nio.file.Paths;\n\npublic class App {\n    public static void main(String[] args) {\n        Pulumi.run(App::stack);\n    }\n\n    public static void stack(Context ctx) {\n        // Read the target gateway's version, compiled-in plugin proof, and topology.\n        final var current = BusbarFunctions.getInfo(%!v(PANIC=Format method: runtime error: invalid memory address or nil pointer dereference);\n\n        ctx.export(\"busbarVersion\", current.version());\n        ctx.export(\"compiledAuthModules\", current.authModules());\n    }\n}\n```\n```yaml\nvariables:\n  # Read the target gateway's version, compiled-in plugin proof, and topology.\n  current:\n    fn::invoke:\n      function: busbar:getInfo\n      arguments: {}\noutputs:\n  busbarVersion: ${current.version}\n  compiledAuthModules: ${current.authModules}\n```\n\u003c!--End PulumiCodeChooser --\u003e\n",
            "outputs": {
                "description": "A collection of values returned by getInfo.\n",
                "properties": {
                    "authModules": {
                        "description": "Auth modules compiled into this binary (the compliance-by-compilation proof).\n",
                        "items": {
                            "type": "string"
                        },
                        "type": "array"
                    },
                    "configPersistence": {
                        "description": "Whether API-applied config changes are durable across restarts (BUSBAR*CONFIG*OVERLAY set) versus live-only.\n",
                        "type": "boolean"
                    },
                    "configVersion": {
                        "description": "Monotonic config version — 0 at boot, +1 per API config apply. Process-local; resets on restart.\n",
                        "type": "integer"
                    },
                    "hookPlugins": {
                        "description": "Hook plugins compiled into this binary.\n",
                        "items": {
                            "type": "string"
                        },
                        "type": "array"
                    },
                    "id": {
                        "description": "The provider-assigned unique ID for this managed resource.",
                        "type": "string"
                    },
                    "models": {
                        "description": "Number of configured model lanes.\n",
                        "type": "integer"
                    },
                    "pools": {
                        "description": "Number of configured pools.\n",
                        "type": "integer"
                    },
                    "providers": {
                        "description": "Number of configured providers.\n",
                        "type": "integer"
                    },
                    "startedAt": {
                        "description": "Epoch seconds of process start — the boot-epoch marker. A changed value means a restart (config_version resets), never a config revert.\n",
                        "type": "integer"
                    },
                    "uptimeSeconds": {
                        "description": "Seconds since the gateway process started; null if never stamped.\n",
                        "type": "integer"
                    },
                    "version": {
                        "description": "busbar semantic version reported by the gateway.\n",
                        "type": "string"
                    },
                    "weightedFloor": {
                        "description": "The inline SWRR floor — always true (compiled in unconditionally).\n",
                        "type": "boolean"
                    }
                },
                "required": [
                    "authModules",
                    "configPersistence",
                    "configVersion",
                    "hookPlugins",
                    "models",
                    "pools",
                    "providers",
                    "startedAt",
                    "uptimeSeconds",
                    "version",
                    "weightedFloor",
                    "id"
                ],
                "type": "object"
            }
        },
        "pulumi:providers:busbar/terraformConfig": {
            "description": "This function returns a Terraform config object with terraform-namecased keys,to be used with the Terraform Module Provider.",
            "inputs": {
                "properties": {
                    "__self__": {
                        "type": "ref",
                        "$ref": "#/provider"
                    }
                },
                "type": "pulumi:providers:busbar/terraformConfig",
                "required": [
                    "__self__"
                ]
            },
            "outputs": {
                "properties": {
                    "result": {
                        "additionalProperties": {
                            "$ref": "pulumi.json#/Any"
                        },
                        "type": "object"
                    }
                },
                "required": [
                    "result"
                ],
                "type": "object"
            }
        }
    }
}